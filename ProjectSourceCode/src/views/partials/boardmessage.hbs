<div class="message" id="message-{{id}}" style="margin-left: {{indentLevel}}em;">
  <strong>{{author}}:</strong>
  <p>{{text}}</p>

  {{#if replies}}
    <div class="replies">
      {{#each replies}}
        {{> boardmessage}}
      {{/each}}
    </div>
  {{/if}}
</div>